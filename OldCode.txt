//Ellipse marker = new()
            //{
            //    Width = 10,
            //    Height = 10,
            //    Fill = new SolidColorBrush(Colors.SlateGray),
            //    Cursor = Cursors.Hand
            //};

            //theCanvas.Children.Add(marker);
            //Canvas.SetLeft(marker, LEFT_PADDING - (marker.Height / 2));
            //Canvas.SetTop(marker, yPosition - (marker.Height / 2));

            //marker.MouseLeftButtonDown += (sender, e) => e.Handled = true;
            //marker.MouseEnter += (sender, e) => marker.Fill = MARKER_HOVER_COLOUR;
            //marker.MouseLeave += (sender, e) => marker.Fill = MARKER_COLOUR;
            //marker.MouseLeftButtonUp += (sender, e) =>
            //{
            //    ItemSelected?.Invoke(this, new TimelineItemSelectedEventArgs(item));
            //};


            //TextBlock timeText = new()
            //{
            //    Text = item.DateTime.ToString("HH:mm:ss"),
            //    Cursor = Cursors.Hand,
            //    Visibility = Visibility.Hidden
            //};

            //theCanvas.Children.Add(timeText);
            //timeText.Measure(new Size(double.PositiveInfinity, double.PositiveInfinity));

            //TextBlock approxTilde = new()
            //{
            //    Text = "~",
            //    Cursor = Cursors.Hand,
            //    Visibility = Visibility.Hidden
            //};

            //theCanvas.Children.Add(approxTilde);
            //approxTilde.Measure(new Size(double.PositiveInfinity, double.PositiveInfinity));

            //// Show only the marker if this item will obstruct the above item
            //if (lastFullItemYPos == 0 || yPosition >= lastFullItemYPos + timeText.DesiredSize.Height)
            //{
            //    lastFullItemYPos = yPosition;

            //    timeText.Visibility = Visibility.Visible;
            //    Canvas.SetLeft(timeText, LEFT_PADDING - timeText.DesiredSize.Width - 15);
            //    Canvas.SetTop(timeText, yPosition - (timeText.DesiredSize.Height / 2));

            //    if (item.IsApproximate)
            //    {
            //        approxTilde.Visibility = Visibility.Visible;
            //        Canvas.SetLeft(approxTilde, Canvas.GetLeft(timeText) - approxTilde.DesiredSize.Width - 3);
            //        Canvas.SetTop(approxTilde, Canvas.GetTop(timeText));
            //    }

            //    timeText.MouseLeftButtonDown += (sender, e) => e.Handled = true;
            //    timeText.MouseEnter += (sender, e) => marker.Fill = MARKER_HOVER_COLOUR;
            //    timeText.MouseLeave += (sender, e) => marker.Fill = MARKER_COLOUR;
            //    timeText.MouseLeftButtonUp += (sender, e) =>
            //    {
            //        ItemSelected?.Invoke(this, new TimelineItemSelectedEventArgs(item));
            //    };

            //    if (item.Text != null && item.Text.Length > 0)
            //    {
            //        Line dash = new()
            //        {
            //            Stroke = new SolidColorBrush(Colors.Black),
            //            StrokeThickness = 2,
            //            X1 = LEFT_PADDING + 15,
            //            X2 = LEFT_PADDING + 30,
            //            Y1 = yPosition,
            //            Y2 = yPosition
            //        };

            //        theCanvas.Children.Add(dash);


            //        TextBlock mainText = new()
            //        {
            //            Text = item.Text,
            //            Width = theCanvas.ActualWidth - LEFT_PADDING - 40,
            //            TextTrimming = TextTrimming.CharacterEllipsis,
            //            Cursor = Cursors.Hand,
            //            LineStackingStrategy = LineStackingStrategy.BlockLineHeight,
            //        };

            //        //mainText.SetBinding(TextBlock.LineHeightProperty, new Binding("FontSize"));
            //        mainText.LineHeight = mainText.FontSize;

            //        theCanvas.Children.Add(mainText);
            //        mainText.Measure(new Size(double.PositiveInfinity, double.PositiveInfinity));
            //        Canvas.SetLeft(mainText, LEFT_PADDING + 40);
            //        Canvas.SetTop(mainText, yPosition - (mainText.DesiredSize.Height / 2) + 1);

            //        mainText.MouseLeftButtonDown += (sender, e) => e.Handled = true;
            //        mainText.MouseEnter += (sender, e) => marker.Fill = MARKER_HOVER_COLOUR;
            //        mainText.MouseLeave += (sender, e) => marker.Fill = MARKER_COLOUR;
            //        mainText.MouseLeftButtonUp += (sender, e) =>
            //        {
            //            ItemSelected?.Invoke(this, new TimelineItemSelectedEventArgs(item));
            //        };
            //    }
            //}